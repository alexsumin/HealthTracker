spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:db/liquibase-changelog.yaml
  jpa:
    properties:
      hibernate:
        format_sql: true
        ddl-auto: none
logging:
  level:
    ROOT: INFO